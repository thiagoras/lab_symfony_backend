{% extends 'base.html.twig' %}

{% block title %}TipoDocumento{% endblock %}

{% block body %}
    <h1>TipoDocumento</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tipo_documento.id }}</td>
            </tr>
            <tr>
                <th>Categoria</th>
                <td>{{ tipo_documento.categoria }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tipo_documento_controller_gen_index') }}">back to list</a>

    <a href="{{ path('app_tipo_documento_controller_gen_edit', {'id': tipo_documento.id}) }}">edit</a>

    {{ include('tipo_documento_controller_gen/_delete_form.html.twig') }}
{% endblock %}
